import React from 'react'

// 이벤트 처리:
// - 웹페이지(컴포넌트)가 사용자와의 상호작용을 받아들여 그것에 대한 처리를 하는 것
// - React에서는 addEventListener 함수를 사용하지 않고 on이벤트 속성을 사용
// - 

export default function EventComponent() {
  return (
    <div>EventComponent</div>
  )
}
